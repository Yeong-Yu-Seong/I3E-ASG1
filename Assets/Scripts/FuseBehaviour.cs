/* 
* Author: Yeong Yu Seong
* Date: 11 June 2025
* Description: This script is used to set the fuse ID of each fuse.
*/
using UnityEngine;

public class FuseBehaviour : MonoBehaviour
{
    /// <summary>
    /// fuseID set to public so that each fuse can have a different ID
    /// </summary>
    [SerializeField]
    public string fuseID;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
